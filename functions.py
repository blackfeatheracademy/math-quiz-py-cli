def get_user_input_int(questin, message, min, ):
    questin_nomber = input (message)
    user_anser = input (questin)
    if user_anser <= max and user_anser >= min:
    else:
        
        print ("you can't do this nomber")
def get_user_input_string(message, allowableList):
    questin_operatin = input (message)